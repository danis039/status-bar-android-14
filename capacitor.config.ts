import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "status.bar.android.v14",
  appName: "status-bar-android-14",
  webDir: "dist",
};

export default config;
